import AppShell from "../components/layout/AppShell";

const ProfilePage = () => {
  return (
    <AppShell
      title="Profile"
      description="Profile feature UI will be implemented in the next phase with authenticated user details."
    />
  );
};

export default ProfilePage;
